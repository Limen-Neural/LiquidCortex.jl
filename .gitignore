Manifest.toml
*.jld2
*.mem
*.log

# Local environment variables (contains secrets — never commit)
.env
.mimocode/

# Local exploration / agent notes (never ship)
.explore-notes/
playground-results/
.cursor/
